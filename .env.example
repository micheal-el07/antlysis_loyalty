# Used by docker-compose.yml only — separate from backend/.env, which is
# for running the backend directly with `npm run dev` (no Docker).
#
#   cp .env.example .env
#   docker compose up --build

DB_NAME=loyalty_program_dev
DB_USER=postgres
DB_PASSWORD=antlysisTest26

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

FRONTEND_URL=http://localhost:5173

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-admin-password
